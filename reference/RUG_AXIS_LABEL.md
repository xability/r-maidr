# What the axis across a rug's ticks is called

Every entry a rug layer emits sits at the same place on that axis, so
whatever the panel calls it measures nothing here. Named rather than
left blank, and named the same thing py-maidr names it, so the two
bindings announce one chart the same way.

## Usage

``` r
RUG_AXIS_LABEL
```
