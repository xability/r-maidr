# Is this panel a volume-only bar panel (single bar layer, no other layers)?

Is this panel a volume-only bar panel (single bar layer, no other
layers)?

## Usage

``` r
is_volume_only_bar_panel(panel)
```
