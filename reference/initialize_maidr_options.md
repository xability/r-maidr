# Initialize MAIDR default options

Sets default values for MAIDR options during package load. Does not
override options already set by the user (e.g. in .Rprofile).

## Usage

``` r
initialize_maidr_options()
```
