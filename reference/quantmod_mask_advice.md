# Advice shown when quantmod masks maidr's chartSeries() wrapper

Shared by \`.onAttach\`, the quantmod attach hook and the "No Base R
plots detected" errors so the wording stays in one place.

## Usage

``` r
quantmod_mask_advice()
```

## Value

A single advice string.
