# Print a ggplot with the original (non-MAIDR) print method

Print a ggplot with the original (non-MAIDR) print method

## Usage

``` r
print_ggplot_natively(x)
```

## Arguments

- x:

  A ggplot object

## Value

NULL (invisible)
