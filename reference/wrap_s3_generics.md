# Wrap S3 generic functions (lines and points)

Special handling for S3 generics that can't be traced normally. Only
called once during .onLoad when namespace is still open.

## Usage

``` r
wrap_s3_generics()
```
