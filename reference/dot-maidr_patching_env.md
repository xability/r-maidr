# Base R Function Patching System

This module provides function patching capabilities for Base R plotting
functions. It intercepts Base R plotting calls and records them for
processing by the MAIDR system.

## Usage

``` r
.maidr_patching_env
```

## Format

An object of class `environment` of length 4.
