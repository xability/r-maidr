# Convert Accuracy to Decimal Places

Converts the scales package `accuracy` parameter to a decimal count. For
example, accuracy = 0.01 becomes decimals = 2.

## Usage

``` r
accuracy_to_decimals(accuracy)
```

## Arguments

- accuracy:

  The accuracy value (e.g., 0.01 for 2 decimal places)

## Value

Integer number of decimal places
