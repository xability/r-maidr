# Get paths to local MAIDR assets

Returns the file paths to the locally bundled MAIDR JavaScript and CSS
files.

## Usage

``` r
maidr_local_assets()
```

## Value

A named list with 'js' and 'css' file paths
