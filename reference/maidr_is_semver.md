# Is a string a semantic version?

Is a string a semantic version?

## Usage

``` r
maidr_is_semver(x)
```

## Arguments

- x:

  Value to test

## Value

`TRUE` for a single string of at most 128 characters in semantic version
form, otherwise `FALSE`
