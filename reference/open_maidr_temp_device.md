# Open a temporary device to suppress default graphics window

Called by wrappers when no device is open to prevent R from opening the
default interactive graphics device.

## Usage

``` r
open_maidr_temp_device()
```

## Value

The device ID of the temp device
