% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_r_arg_utils.R
\name{recorded_main_title}
\alias{recorded_main_title}
\title{The \code{main} title a recorded call wrote, as text}
\usage{
recorded_main_title(args)
}
\arguments{
\item{args}{Recorded argument list}
}
\value{
Character scalar, empty when there is no usable title
}
\description{
\code{main = expression(alpha^2)} is an ordinary way to put a Greek letter on a
chart, and a dozen readers passed the recorded value straight into the
layer's \code{title}. \code{jsonlite::toJSON()} has no method for an expression, so
the whole save failed on a title. A title that is not text is announced
as empty rather than failing the chart it sits on; the drawing keeps it.
}
\details{
Exact-name lookup, since \code{args$main} would partial-match nothing today but
is the same shape as the \code{args$x} / \code{xlab} collision that emptied
\code{monthplot()} (#292).
}
\keyword{internal}
