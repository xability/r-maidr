% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot2_gantt_layer_processor.R
\name{segment_lane_axis}
\alias{segment_lane_axis}
\title{Which axis a layer's segments lay their lanes on}
\usage{
segment_lane_axis(built_data)
}
\arguments{
\item{built_data}{A layer's computed data, carrying \code{x}, \code{xend}, \code{y} and
\code{yend}, one row per drawn segment}
}
\value{
\code{"y"} when the lanes run up the y axis and the spans along x,
\code{"x"} for the mirror image, or \code{NULL} when the layer is not a gantt
}
\description{
A segment whose two ends share a coordinate is a \strong{span} along the other
axis, at one position on this one -- an interval in a lane, which is a
gantt. A segment whose ends share nothing is an edge in a node-link
diagram: it has no lane to sit in and no interval to announce.
}
\details{
The question is asked of the \strong{whole layer} rather than of each row, which
is the rule xability/maidr#1100 settled for the same reading in the
Observable adapter. One \code{geom_segment()} call can hold spans and edges
together, and reading three spans out of four segments would announce a
gantt quietly missing a quarter of its chart.

A layer whose segments are level on \emph{both} axes is every span reduced to a
point. That is not a schedule with milestones in it -- a milestone sits in
a lane beside intervals that have length -- so it is refused rather than
announced as a chart of zero-length work.
}
\keyword{internal}
