% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_r_arg_utils.R
\name{formula_two_way_table}
\alias{formula_two_way_table}
\title{The table \code{mosaicplot.formula()} would build from a recorded formula call}
\usage{
formula_two_way_table(args)
}
\arguments{
\item{args}{Recorded argument list, or NULL}
}
\value{
A table, or NULL when the call is not a readable formula call
}
\description{
\code{mosaicplot(~ Hair + Eye, data = df)} is a common calling style, and the
table it draws is recoverable rather than invented -- so it is built the
way \code{mosaicplot.formula()} builds it, by the same two branches, rather
than by a rule of our own that would agree with it only sometimes.
}
\details{
\subsection{Which branch, and why it matters}{

Read from \code{graphics:::mosaicplot.formula} rather than assumed. A \code{data}
that is a table (or has more than two dimensions) is summed over the
formula's terms; anything else goes through \code{model.frame()} and is
\strong{counted by row}:

\preformatted{
if (inherits(edata, "ftable") || inherits(edata, "table") ||
    length(dim(edata)) > 2) {
  data <- marginSums(as.table(data), varnames)
  mosaicplot(data, ...)
} else {
  mf <- eval(model.frame(formula, data, subset, na.action))
  mosaicplot(table(mf), ...)
}
}

The distinction is not cosmetic, and #248 proposed the other reading. A
data frame of \emph{pre-counted} cells -- \code{as.data.frame(HairEyeColor[, , 1])},
sixteen rows and a \code{Freq} column -- is counted by row like any other, so
the chart draws sixteen equal cells:

\preformatted{
table(model.frame(~ Hair + Eye, df))    xtabs(Freq ~ Hair + Eye, df)
        Brown Blue Hazel Green                  Brown Blue Hazel Green
  Black     1    1     1     1            Black    32   11    10     3
  Brown     1    1     1     1            Brown    53   50    25    15
  Red       1    1     1     1            Red      10   10     7     7
  Blond     1    1     1     1            Blond     3   30     5     8
}

The left table is what \code{mosaicplot()} draws; the right is the one it would
draw if handed \code{HairEyeColor[, , 1]} directly. Reading the right one would
announce numbers the chart does not show, which is the one thing this
processor exists not to do. There is no \code{Freq} convention to match:
\code{mosaicplot.formula()} has none.
}

\subsection{What is declined}{

A recorded \code{subset}. \code{mosaicplot.formula()} passes it into
\code{model.frame()}, so honouring it means reproducing an argument whose
recorded form is not the expression \code{model.frame()} is given -- and
ignoring it would read rows the chart left out. Declined rather than
guessed, which leaves the figure exactly the picture it is today.

A recorded \code{na.action} is \emph{not} passed through, and does not need to be:
\code{table()} drops a missing level whatever reached it, so the three actions
a caller can name all draw the same chart. Measured on a frame with an NA
in each column, \code{na.omit}, \code{na.pass} and \code{na.exclude} gave one table:

\preformatted{
     b
a     p q
  x   2 1
  y   1 0
}

\code{stats::na.omit} is named here anyway, because that is what
\code{mosaicplot.formula()} defaults to and matching it costs nothing.
}
}
\keyword{internal}
