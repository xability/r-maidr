% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knitr_support.R
\name{create_knitr_iframe}
\alias{create_knitr_iframe}
\title{Wrap a chart in its iframe for a knitted document}
\usage{
create_knitr_iframe(content)
}
\arguments{
\item{content}{The chart's SVG content, from \code{\link[=create_maidr_html]{create_maidr_html()}}}
}
\value{
Character string of iframe HTML
}
\description{
Online, the frame loads maidr.js from the CDN, and the document is given
its own copy of the bundle (\code{\link[=maidr_page_bundle_dependency]{maidr_page_bundle_dependency()}}) for the frame
to fall back on. The frame's document sits in a \code{srcdoc} attribute, where
R Markdown's \code{self_contained} and Quarto's \code{embed-resources} cannot reach
its \verb{<script src>}; the copy is what they embed instead, once per document
however many charts it has, so a self-contained document's charts work
offline. Offline at render time, each frame carries the bundle inline, as
before.
}
\keyword{internal}
