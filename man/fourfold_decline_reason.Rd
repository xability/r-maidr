% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_r_adapter.R
\name{fourfold_decline_reason}
\alias{fourfold_decline_reason}
\title{Which measured refusal a \code{fourfoldplot()} call runs into, if any}
\usage{
fourfold_decline_reason(args)
}
\arguments{
\item{args}{The arguments recorded from the \code{fourfoldplot()} call.}
}
\value{
NULL when the counts are readable, otherwise \code{"std"}, \code{"strata"}
or \code{"table"}.
}
\description{
Returns NULL when the call is read, and otherwise the reason
\code{warn_fourfoldplot_declined()} explains. Kept beside \code{is_two_way_table()}
and called from the processor as well as from dispatch, so the two cannot
disagree about which calls are readable.
}
\details{
The three gates, in the order they are asked:
\enumerate{
\item \strong{\code{std}.} Only \code{"ind.max"} and \code{"all.max"} are read. Measured on
\verb{c(tab) = 10, 40, 90, 160}, \code{std = "ind.max"} draws radii
\verb{0.25, 0.50, 0.75, 1.00} and \code{r^2 * max(count)} recovers
\verb{10, 40, 90, 160} exactly, so the wedge AREA is the count. Under the
default \code{"margins"} the same table draws
\verb{0.632456, 0.774597, 0.774597, 0.632456} -- \code{r1 == r4}, \code{r2 == r3},
four radii carrying one number.
\item \strong{Shape.} Exactly two dimensions, both of extent 2. Written as
\code{length(dims) == 2L && all(dims == 2L)} and NOT as
\code{identical(dims, c(2L, 2L))}, because \code{dim()} may carry the dimension
names and \code{identical()} compares them -- so the exact-comparison
spelling can decline a table for a reason that has nothing to do with
what the chart draws. Measured, \code{dim(as.table(ftable(tb)))} is
\code{c(Treatment = 2L, Outcome = 2L)} on R 4.3.3 and unnamed on R 4.6.1, so
which tables that spelling would have dropped varies by R version. The
spelling used here does not vary, which is the point of it: whether the
names survive is not a fact about the chart.
\item \strong{Values.} \code{is.numeric()} rather than \code{as.numeric()}: measured, a
logical 2x2 prints \code{TRUE}/\code{FALSE} on the page as its count labels while
\code{as.numeric()} would have announced \code{1}/\code{0} under \code{z = "Count"}.
Finite, non-negative and summing above zero, because the all-zero table
makes \code{stdize()} return \code{NaN} four times and grid emits ZERO polygon
grobs for it -- measured, \code{npoly = 0}.

\code{all(counts >= 0)} is load-bearing, not defensive, and the obvious
reading of it is wrong. An \code{NA} count does stop \code{fourfoldplot()} under
every \code{std}, with "missing value where TRUE/FALSE needed", so it never
arrives. A NEGATIVE count stops only under the DEFAULT
\code{std = "margins"}, with that same message. Measured on
\code{c(-1, 2, 3, 4)}, \code{std = "ind.max"} and \code{std = "all.max"} both return
normally with a "NaNs produced" warning and \code{npoly = 0} -- and those
are exactly the two values that get past gate 1. So a negative count
reaches this gate whenever it is drawable at all, its counts are finite
and sum to 8, and this clause is the one that declines it.
\code{test-base-r-fourfoldplot.R} pins both halves.
}

A 2x2xk array fails gate 2 twice over, and is asked about first only so
the advisory can say "strata" rather than "not a 2x2 table":
\code{recorded_two_way_table()} returns NULL for any input with three
dimensions anyway. That includes \verb{2x2x1}, which draws exactly what the
matrix spelling draws -- a conservative, measured loss.
}
\keyword{internal}
