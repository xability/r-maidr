% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot2_jitter.R
\name{undisplaced_layer_data}
\alias{undisplaced_layer_data}
\title{The positions a layer's points would have without its jitter}
\usage{
undisplaced_layer_data(plot, layer_index)
}
\arguments{
\item{plot}{The ggplot2 object.}

\item{layer_index}{Index of the layer to undisplace.}
}
\value{
A data frame of the layer's undisplaced built data, or \code{NULL} when
the rebuild fails or does not line up row for row with the original.
}
\description{
Rebuilds \code{plot} with the one layer's position adjustment replaced by
\code{position_identity()} and returns that layer's built data.
}
\details{
The replacement is a fresh \code{ggproto} child rather than an assignment to the
layer's own \code{position} field. ggplot2 \code{Layer} objects are \code{ggproto} and have
\strong{reference} semantics, so \code{plot$layers[[i]]$position <- ...} would alter
the caller's plot -- the object they still hold and may draw again. A child
shadows the field instead, leaving the parent untouched; asserted in the
tests rather than left as a claim.
}
\keyword{internal}
