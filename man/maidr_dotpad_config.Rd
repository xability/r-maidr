% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dotpad_config.R
\name{maidr_dotpad_config}
\alias{maidr_dotpad_config}
\title{Where maidr.js loads the DotPad SDK from}
\usage{
maidr_dotpad_config()
}
\value{
A list with \code{sdk_url} and \code{asset_base_url}, each a single string
or \code{NULL} when unset.
}
\description{
maidr.js does not bundle the DotPad tactile-display SDK: it ships without a
licence permitting redistribution, so the first time a reader connects a
DotPad, maidr.js imports the SDK from the vendor's published copy on
jsDelivr. That is the one path an offline document (\code{use_cdn = FALSE})
still takes to the network. The document renders, sonifies and brailles
without it; only connecting a DotPad needs it, unless the page names its
own copy of the SDK.
}
\details{
maidr.js reads two globals off the page before it loads:
\code{window.MAIDR_DOTPAD_SDK_URL}, the SDK ES module, and
\code{window.MAIDR_DOTPAD_ASSET_BASE_URL}, the directory holding the braille
engine's \code{liblouis.js}, \code{.wasm} and \code{.data} files (by default the \verb{lib/}
folder beside the module). This reads the R-side settings for them: the
options \code{maidr.dotpad_sdk_url} and \code{maidr.dotpad_asset_base_url}, falling
back to the environment variables \code{MAIDR_DOTPAD_SDK_URL} and
\code{MAIDR_DOTPAD_ASSET_BASE_URL}, which carry the same names as the globals.
An empty value counts as unset.
}
\seealso{
\link{maidr-options}
}
\keyword{internal}
