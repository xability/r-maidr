% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vioplot_stats.R
\name{extract_vioplot_samples}
\alias{extract_vioplot_samples}
\title{Split a vioplot call's arguments into one sample per violin}
\usage{
extract_vioplot_samples(args)
}
\arguments{
\item{args}{The recorded call's arguments.}
}
\value{
A named list of numeric vectors, one per violin, or an empty list.
}
\description{
\code{vioplot()} takes its groups the way \code{boxplot()} does: as separate vectors
(\code{vioplot(a, b, c)}), as a single list or data frame, or as a formula. Only
the first two are read here; a formula call is left for the caller to
decline, because resolving it needs the environment the call was made in and
reconstructing that would be guesswork.
}
\keyword{internal}
