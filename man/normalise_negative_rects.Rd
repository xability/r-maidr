% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svg_utils.R
\name{normalise_negative_rects}
\alias{normalise_negative_rects}
\title{Restate a rect grob's negative heights and widths as positive ones}
\usage{
normalise_negative_rects(grob)
}
\arguments{
\item{grob}{A grob, gTree, gList, or gtable (or NULL)}
}
\value{
The same tree with every rect's extent stated positively
}
\description{
\code{gridSVG::grid.export()} warns "number of items to replace is not a
multiple of replacement length" on any rect grob drawn with a negative
height or width, and the warning reaches \code{save_html()}, where a plot that
warns falls back to a picture.
}
\details{
Measured on a bare \code{rectGrob()} with nothing else to it: four rects with
positive heights export silently, and the same four with two heights
negated warn. So this is an upstream gridSVG defect rather than anything
about a particular chart -- but it is reached by an ordinary one.
\code{assocplot()} draws every tile from a baseline with \code{just = c("left", "bottom")} and a signed height, so a cell below expectation is a negative
height by construction (#266); every association plot would warn.

A rect at \verb{(y, h)} with \code{h < 0} covers the same pixels as one at
\verb{(y + h, |h|)}, so the drawing is unchanged -- only the arithmetic gridSVG
does with it. The same holds for \code{x} and a negative width.

Drop this repair if gridSVG ever handles negative dimensions itself.
}
\keyword{internal}
