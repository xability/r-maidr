% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_r_arg_utils.R
\name{recorded_flag}
\alias{recorded_flag}
\title{Read a recorded logical argument the way its drawing function does}
\usage{
recorded_flag(args, name, default = FALSE)
}
\arguments{
\item{args}{Recorded argument list}

\item{name}{The formal's name}

\item{default}{What an absent, \code{NA} or unreadable argument means}
}
\value{
TRUE or FALSE
}
\description{
Every base R reader asked \code{isTRUE()} of a recorded flag, and the base R
drawing functions ask \verb{if (x)}. The two agree on \code{TRUE}, on \code{FALSE} and on
absent, and disagree on every other truthy value R accepts in an \code{if} --
so a chart written \code{stripchart(x, vertical = 1)} was drawn vertically and
announced horizontally, with the values on the group axis and the group
positions on the value axis, silently, on a chart that renders as an
interactive one rather than as a fallback (#256).
}
\details{
Measured, by reading each drawing function's own body:\tabular{ll}{
   function \tab asks \cr
   \code{barplot.default} \tab \verb{if (beside)}, \code{(logx && horiz)} \cr
   \code{bxp} \tab \verb{if (horizontal)} \cr
   \code{hist.default} \tab \verb{if (freq1)} \cr
   \code{stripchart.default} \tab \verb{if (vertical)} \cr
   \code{qqnorm.default}, \code{qqline} \tab \verb{if (datax)} \cr
   \code{vioplot.default} \tab `if (horizontal \cr
}


All seven ask R's own truthiness, so all seven are read through this.

\code{NA} and an uncoercible value give the caller's default rather than an
error: \verb{if (NA)} stops in R, but a reader that stops takes the whole
figure with it, and a chart read under its default is better than no chart
at all. A value of any length but one does the same, since \code{if} on one of
those errors too.
}
\keyword{internal}
