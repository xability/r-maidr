% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dotpad_config.R
\name{maidr_dotpad_sdk_dependency}
\alias{maidr_dotpad_sdk_dependency}
\title{A downloaded SDK as an htmltools dependency}
\usage{
maidr_dotpad_sdk_dependency(dir = maidr_dotpad_sdk_dir(), libdir = "lib")
}
\arguments{
\item{dir}{A complete copy, as \code{\link[=maidr_dotpad_sdk_available]{maidr_dotpad_sdk_available()}} reports}

\item{libdir}{The \code{libdir} the document is saved with}
}
\value{
An \code{htmltools::htmlDependency()}
}
\description{
For the documents \code{show()} and \code{save_html()} write. htmltools copies the
directory into \verb{<libdir>/dotpad-sdk-<version>/} when the document is saved, and
the dependency's \code{head} declares the two globals with that relative path,
so the saved page finds its copy wherever the folder is moved to, as long
as the two move together. \code{libdir} is what \code{\link[htmltools:save_html]{htmltools::save_html()}} is
called with; its default is what this package uses.
}
\keyword{internal}
