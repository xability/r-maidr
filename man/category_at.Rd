% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot2_category_labels.R
\name{category_at}
\alias{category_at}
\title{The name at one position, or \code{NULL}}
\usage{
category_at(position, labels)
}
\arguments{
\item{position}{A drawn coordinate}

\item{labels}{The map from \code{\link[=discrete_axis_labels]{discrete_axis_labels()}}}
}
\value{
The name, or \code{NULL} when there is none
}
\description{
Positions are matched exactly rather than by rounding. ggplot2 places a
discrete category on an integer, and a point drawn \emph{off} one has been moved
there deliberately -- a \code{position_dodge} splitting a group, a jitter that
was not undone. Naming it after the nearest tick would claim a category the
renderer did not put it in, which is the failure this exists to prevent
rather than a smaller version of it.
}
\keyword{internal}
