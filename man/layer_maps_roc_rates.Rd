% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot2_roc_layer_processor.R
\name{layer_maps_roc_rates}
\alias{layer_maps_roc_rates}
\title{Whether a line layer maps the ROC's own vocabulary}
\usage{
layer_maps_roc_rates(layer, plot_object)
}
\arguments{
\item{layer}{A ggplot2 layer}

\item{plot_object}{The plot the layer belongs to}
}
\value{
TRUE when the layer's x and y are specificity and sensitivity
}
\description{
A ROC curve drawn as \code{geom_line()} or \code{geom_path()} carries no evidence of
what it means except its column names, and two producers name them after
the rates themselves: \code{\link[pROC:ggroc]{pROC::ggroc()}} maps \code{specificity} or
\code{1-specificity} against \code{sensitivity}, and \code{ggplot2::autoplot()} of a
\code{\link[yardstick:roc_curve]{yardstick::roc_curve()}} maps \code{1 - specificity} against \code{sensitivity}.
Those names are the claim, and nothing else is read as one -- a line of
\code{fpr} against \code{tpr} under any other column names is declared with
\code{\link[=maidr_roc]{maidr_roc()}} instead, because a rule loose enough to catch it would catch
charts that are not ROC curves.
}
\keyword{internal}
