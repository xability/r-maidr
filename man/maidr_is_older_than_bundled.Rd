% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdn_version.R
\name{maidr_is_older_than_bundled}
\alias{maidr_is_older_than_bundled}
\title{Is a resolved version older than the bundled one?}
\usage{
maidr_is_older_than_bundled(resolved, bundled = MAIDR_VERSION)
}
\arguments{
\item{resolved}{The version the resolver answered with}

\item{bundled}{The version bundled with this package}
}
\value{
\code{TRUE} when \code{resolved} sorts below \code{bundled}
}
\description{
Semantic version precedence, as py-maidr's \verb{_is_older_than_bundled}
applies it: MAJOR.MINOR.PATCH compared numerically with
\code{\link[=numeric_version]{numeric_version()}}; on a tie, a pre-release sorts below the release it
precedes (\verb{4.9.0-rc.1} < \verb{4.9.0}), and two pre-releases compare
identifier by identifier, numeric ones numerically and below alphanumeric
ones, alphanumeric ones in ASCII order, a shorter run below a longer one
it begins. Build metadata (\code{+build.5}) carries no precedence and is
ignored.
}
\details{
A version that cannot be compared is not called older: the resolver's
answer, already checked to be a semantic version, stays in place.
}
\keyword{internal}
