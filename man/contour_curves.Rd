% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot2_contour_layer_processor.R
\name{contour_curves}
\alias{contour_curves}
\title{Group a contour layer's rows into the curves it drew}
\usage{
contour_curves(built_data)
}
\arguments{
\item{built_data}{A layer's computed data, carrying \code{x}, \code{y}, \code{level} and
\code{piece}, one row per vertex}
}
\value{
A list with \code{data} (one curve per piece, each a list of \code{x}, \code{y}
and \code{level}) and \code{order} (the piece behind each emitted curve)
}
\description{
A contour draws a scalar field as curves of constant value, and
\code{ggplot_build()} has already done the hard half: \code{level} is a number on
every row, and \code{piece} separates the curves. A field with two peaks crosses
a level twice and arrives as two pieces, so nothing has to be split apart
here -- which is the one place this reading is easier than the matplotlib
one, where both islands come back in a single compound path
(xability/py-maidr#540).
}
\details{
Pieces are emitted in ascending \code{piece}, which is ascending level and then
draw order within a level, and the returned \code{order} is that sequence of
piece identifiers -- what the selectors are built from, so the highlight
follows the grouping rather than relying on the document happening to agree.
}
\keyword{internal}
