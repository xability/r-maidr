% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/series_group_utils.R
\name{resolve_series_group_names}
\alias{resolve_series_group_names}
\title{Name each series after the category its built group id stands for}
\usage{
resolve_series_group_names(plot, group_ids, column = "group")
}
\arguments{
\item{plot}{The ggplot2 object}

\item{group_ids}{The layer's built \code{group} column}

\item{column}{Name of the mapped grouping column}
}
\value{
Character vector, one name per distinct group id in ascending order
}
\description{
\code{ggplot_build()} replaces the grouping column with integer group ids,
so the user-facing name has to be recovered from the plot's own data. The
ids are assigned in the sorted order of the grouping column's values, which
is the order this function relies on. Falls back to "Series <id>" when the
mapped column is not present on the plot data (for example an expression
such as \code{aes(colour = paste(a, b))}).
}
\keyword{internal}
