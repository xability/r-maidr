% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svg_utils.R
\name{flatten_single_selectors}
\alias{flatten_single_selectors}
\title{Collapse a layer's selector list into the one string its type is read as}
\usage{
flatten_single_selectors(node)
}
\arguments{
\item{node}{A maidr-data node (list, or a leaf)}
}
\value{
The node with single-selector layers carrying a string
}
\description{
Every processor builds \code{selectors} with \code{list()} or \code{lapply()}, so even a
single CSS selector reaches \code{jsonlite::toJSON()} as a list of one, and
\code{auto_unbox = TRUE} cannot unbox a list: the payload says
\verb{"selectors": ["#geom_rect\\\\.rect\\\\.2\\\\.1 rect"]}. maidr.js 4.x reads that
array as one selector per data point, resolves it to one element for
seven bars, and declines the whole layer -- navigation and speech keep
working while nothing on the chart ever changes colour (#316). Measured
in headless Chromium against the bundled 4.9.0: ggplot2 bar, point,
histogram, dodged, stacked and pie, and Base R \code{barplot()}, \code{hist()},
\code{plot()} and \code{pie()}, all announced their values and drew no highlight;
rewriting only the JSON to a string restored every one of them.
}
\details{
For a layer whose type is in \link{SINGLE_SELECTOR_LAYER_TYPES}, a flat
character vector or list of strings becomes one string. Several entries
are joined with \code{", "}: a selector list, which \code{querySelectorAll()}
resolves in document order -- the same order the layer's points are in,
since a processor that names several containers (Base R \code{pie()} draws
one polygon grob per wedge) walks them in drawing order. Nested lists (a
per-cell grid), named objects (\code{BoxSelector}) and anything not made of
strings are left exactly as they are, and so is every other layer type.

Applied where \code{\link[=drop_empty_selectors]{drop_empty_selectors()}} is, and for the same reason:
every payload passes through this one point, the processors are honest
about what they found, and the shape the frontend reads is decided once,
against the bundle actually shipped, where a future bundle bump has one
place to look.
}
\keyword{internal}
