% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discrete_level_utils.R
\name{level_keys}
\alias{level_keys}
\title{Key aesthetic values so a missing one stays distinct from the string "NA"}
\usage{
level_keys(values)
}
\arguments{
\item{values}{A vector of aesthetic values}
}
\value{
Character vector of keys, one per value, never \code{NA}
}
\description{
\code{paste()} stringifies \code{NA} to "NA", which collapses a missing value onto a
level that is literally those two characters -- so a grid keyed that way
cannot tell the two apart, and a duplicate test built on it does not see
the collision. Every present value is prefixed with "=", so the missing
key is one nothing else can produce.
}
\details{
The missing key is a non-empty string on purpose. \code{""} would read as the
obvious sentinel and is a trap: R lets \code{"" \%in\% names(x)} answer TRUE and
then throws "subscript out of bounds" on \code{x[[""]]}, because empty is how a
name-less element is spelled. A lookup guarded by \code{\%in\%} -- which is how
both bar processors read their cells -- would pass the guard and abort.
}
\keyword{internal}
