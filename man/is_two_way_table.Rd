% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_r_adapter.R
\name{is_two_way_table}
\alias{is_two_way_table}
\title{Whether a \code{mosaicplot()} call was handed a two-way table}
\usage{
is_two_way_table(args)
}
\arguments{
\item{args}{The arguments recorded from the \code{mosaicplot()} call.}
}
\value{
\code{TRUE} when the call's table has exactly two dimensions.
}
\description{
A \code{mosaic} layer has one category axis and one fill, so it can carry a
two-dimensional table and no more. \code{mosaicplot()} accepts deeper ones and
splits them recursively.
}
\details{
The table itself is resolved by \code{recorded_two_way_table()}, which the
processor also reads, so dispatch and extraction cannot disagree about
which calls are readable.
}
\keyword{internal}
