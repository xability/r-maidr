% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svg_utils.R
\name{SINGLE_SELECTOR_LAYER_TYPES}
\alias{SINGLE_SELECTOR_LAYER_TYPES}
\title{Layer types whose frontend model reads \code{selectors} as ONE selector for
every mark}
\usage{
SINGLE_SELECTOR_LAYER_TYPES
}
\description{
In maidr.js 4.x the shape of \code{selectors} is a contract, not a
convenience. A plain string is handed to \code{document.querySelectorAll()}
and the matches are aligned to the layer's points in document order. An
ARRAY means something else for these types: one selector per data point
for \code{bar} and \code{hist} (\code{src/model/bar.ts}), a per-series grid for the
segmented bars and \code{mosaic} (\code{src/model/segmented.ts}), and nothing at all
for \code{point} and \code{pie}, whose models read \verb{layer.selectors as string}
(\code{src/model/scatter.ts}, \code{src/model/pie.ts}). \code{dot} and \code{lollipop} are
built by the \code{bar} model; \code{heat} reads a string or a per-cell grid
(\code{src/model/heatmap.ts}). maidr.js 3.x read a one-element array as the
string it held, which is why every processor here that returns
\code{list(selector)} was fine until the bundle moved to 4.0 (#316).
}
\details{
Types NOT listed keep whatever shape their processor built, because their
models want the array: one selector per series for \code{line}, \code{smooth},
\code{step}, \code{area}, \code{roc} and \code{violin_kde}, one per level for \code{contour}, one
per box for \code{box} and \code{violin_box}, one per tick for a rug, and \code{error_bar},
\code{gantt}, \code{word_cloud} and \code{candlestick} accept either.
}
\keyword{internal}
