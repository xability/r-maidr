% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_r_arg_utils.R
\name{recorded_formula_frame}
\alias{recorded_formula_frame}
\title{Resolve a recorded formula into the frame the chart was drawn from}
\usage{
recorded_formula_frame(args, call_env = NULL)
}
\arguments{
\item{args}{Recorded argument list}

\item{call_env}{The environment snapshot a deferred call was recorded
with, or NULL when every argument is a plain value.}
}
\value{
The model frame, or NULL when the call carries no formula or the
frame cannot be built -- in which case the reader falls back to
resolving it itself, exactly as before.
}
\description{
Base R calls are recorded and read later, at \code{show()}/\code{save_html()} time,
and for every argument but one that is harmless: the wrapper records
\emph{evaluated values}, so a vector recorded is a vector and rebinding the
name it came from afterwards changes nothing.
}
\details{
A formula is the exception. It is a reference rather than a value -- it
carries the environment it was written in -- and a processor that calls
\code{stats::model.frame()} on it at render time resolves the variables
\strong{then}. Measured (#254):

\if{html}{\out{<div class="sourceCode">}}\preformatted{len  <- c(1, 2, 3, 10, 11, 12); supp <- rep(c("OJ", "VC"), each = 3)
stripchart(len ~ supp)              # draws 1,2,3 under OJ
len  <- c(99, 98, 97, 96, 95, 94)   # the user carries on working
supp <- rep(c("XX", "YY"), each = 3)
save_html(file = f)                 # announced 99,98,97 under XX
}\if{html}{\out{</div>}}

Every value and both group names belonged to bindings made after the
drawing, and it was silent: the figure rendered as an interactive chart
rather than as a fallback, so nothing said the numbers had moved.

So the frame is built \strong{here}, while the call is being recorded and the
bindings are still the ones the chart was drawn from. \code{stripchart.formula}
and \code{boxplot.formula} build the same \code{stats::model.frame(formula, data)}
as they draw, so this is the frame they used rather than a reconstruction
of it.

Fixed at the recording layer rather than per processor because anything
that reads a formula later inherits the same defect.
}
\keyword{internal}
