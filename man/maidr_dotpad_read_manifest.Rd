% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dotpad_config.R
\name{maidr_dotpad_read_manifest}
\alias{maidr_dotpad_read_manifest}
\title{Parse the shipped DotPad SDK manifest}
\usage{
maidr_dotpad_read_manifest(path = NULL)
}
\arguments{
\item{path}{Where to read from. Defaults to the installed \verb{inst/} copy.}
}
\value{
The manifest, in the shape \code{\link[=maidr_dotpad_sdk_manifest]{maidr_dotpad_sdk_manifest()}} returns.
}
\description{
Split from \code{\link[=maidr_dotpad_sdk_manifest]{maidr_dotpad_sdk_manifest()}} so the read happens once and the
parsing is testable on its own. Every field is checked, because the file
is not authored here: \code{fetch-maidr-bundle.sh} copies whatever
\code{dist/dotpad-sdk.json} the pinned \code{maidr.js} release ships. A field that
changed shape upstream is named as a bad manifest rather than surfacing
later as a \code{vapply} type error.
}
\keyword{internal}
