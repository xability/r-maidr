% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svg_utils.R
\name{split_vectorised_curve_grobs}
\alias{split_vectorised_curve_grobs}
\title{Split a vectorised \code{curve} grob into one curve per row}
\usage{
split_vectorised_curve_grobs(grob)
}
\arguments{
\item{grob}{A grob, gTree, gList, or gtable (or NULL)}
}
\value{
The same tree with every multi-row \code{curve} grob split row-wise
}
\description{
\code{geom_curve()} draws every row of its layer as a single \code{curve} grob whose
positions \emph{and} whose \code{gp} are vectors -- measured on ggplot2 3.4.4, a
four-row layer arrives as \code{GRID.curve.1} with \code{x1}, \code{y1}, \code{x2}, \code{y2} and
\code{gp$col}, \code{gp$fill}, \code{gp$lwd}, \code{gp$lty} all of length 4. \code{gridSVG}'s
\code{svgStyleAttributes()} rejects that outright --
"All SVG style attribute values must have length 1" -- so
\code{gridSVG::grid.export()} aborts on the whole plot and no curve chart could
be read at all (#195).
}
\details{
A \code{segments} grob is equally vectorised and exports fine, because gridSVG
has a method that splits it into one element per segment. There is no such
method for \code{curve}, so the split is done here instead: each row becomes its
own \code{curve} grob carrying its own slice of the gpar, gathered under a gTree
keeping the original's name. The drawing is unchanged -- every row is drawn
with exactly the styling it had -- and the export gains one element per
row, which is what a gantt's selectors address.

Slicing rather than scalarising is the point. Taking \code{gp[[1]]} would also
satisfy gridSVG and is visibly wrong: measured on a layer coloured by a
third column, the four rows export as \code{rgb(248,118,109)}, \code{rgb(0,186,56)},
\code{rgb(97,156,255)} and \code{rgb(0,186,56)}, so collapsing to the first would
paint the whole layer red.

This is an upstream gridSVG gap rather than anything maidr introduced; drop
the split if gridSVG ever gains a \code{curve} method of its own.
}
\keyword{internal}
