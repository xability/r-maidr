% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dotpad_config.R
\name{maidr_download_dotpad_sdk}
\alias{maidr_download_dotpad_sdk}
\title{Download the DotPad SDK for use offline}
\usage{
maidr_download_dotpad_sdk(
  dir = maidr_dotpad_sdk_dir(),
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{dir}{Where to write. Defaults to the option \code{maidr.dotpad_sdk_dir},
the environment variable \code{MAIDR_DOTPAD_SDK_DIR}, or a per-user cache
directory.}

\item{force}{Refetch files that are already present and correct.}

\item{quiet}{Say nothing about what was fetched.}
}
\value{
The directory, invisibly.
}
\description{
maidr.js drives a \href{https://maidr.ai/docs/TACTILE_DISPLAY.html}{DotPad tactile display}
through the vendor's SDK, which it does not bundle: the braille engine
inside it is a 14 MB liblouis build, and every document would carry it for
the few readers who own the device. By default maidr.js imports the
vendor's published copy from jsDelivr, pinned to a commit, the first time
a DotPad is connected -- the one path an offline document
(\code{use_cdn = FALSE}) still takes to the network.
}
\details{
This fetches that pinned copy once -- the module, the liblouis build, and
the LGPL licence text and wrapper sources the vendor asks redistributors to
keep beside it -- verifying every file against its recorded size and MD5,
and writes a \code{manifest.json} beside them naming the commit they came from.
From then on \code{\link[=show]{show()}} and \code{\link[=save_html]{save_html()}} copy it into \verb{lib/dotpad-sdk-3.0.2/}
next to every \code{use_cdn = FALSE} document and tell maidr.js where it is, so
a reader connects a DotPad without the network. A file already present and
correct is left alone, so a second call costs nothing.

A page served from somewhere else -- an intranet host, or a knitr document,
whose charts live in \code{srcdoc} frames with no base URL for a relative path
to resolve against -- names its copy by URL instead, through the options
\code{maidr.dotpad_sdk_url} and \code{maidr.dotpad_asset_base_url}; see
\link{maidr-options}. A configured URL wins over a downloaded copy.
}
\examples{
\dontrun{
maidr_download_dotpad_sdk() # about 14 MB, once
save_html(p, "chart.html", use_cdn = FALSE) # carries the SDK in lib/
}
}
\seealso{
\link{maidr-options} for naming a copy by URL
}
