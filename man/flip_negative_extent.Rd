% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svg_utils.R
\name{flip_negative_extent}
\alias{flip_negative_extent}
\title{Move a rect's anchor so its extent can be stated positively}
\usage{
flip_negative_extent(position, extent, anchor)
}
\arguments{
\item{position}{The rect's \code{x} or \code{y}, as a unit}

\item{extent}{The rect's \code{width} or \code{height}, as a unit}

\item{anchor}{Where the rect is anchored on this axis, from
\code{\link[=rect_anchor]{rect_anchor()}}: 0 is the low edge}
}
\value{
List with the restated \code{position} and \code{extent}
}
\description{
Only rects anchored at the low edge are touched. A centred or
high-anchored rect with a negative extent covers a different span, and
moving its anchor would move the rectangle rather than restate it -- so
those are left as they are, warning and all, rather than silently redrawn
somewhere else.
}
\keyword{internal}
