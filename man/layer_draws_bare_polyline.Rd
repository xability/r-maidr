% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grob_utils.R
\name{layer_draws_bare_polyline}
\alias{layer_draws_bare_polyline}
\title{Does a layer's curve land in the panel's auto-named polyline population?}
\usage{
layer_draws_bare_polyline(layer, type)
}
\arguments{
\item{layer}{A ggplot2 layer.}

\item{type}{The layer type the adapter detected for it.}
}
\value{
\code{TRUE} when the layer draws a bare, auto-named polyline.
}
\description{
\code{layer_polyline_grobs()} keeps every polyline that no \emph{geom-named} tree
claims, so the population it returns is "layers that draw a BARE polyline"
-- which is not the same set as "layers typed line". \code{geom_function()} is
typed \code{smooth} and draws one anyway: \code{GeomFunction} inherits
\code{GeomPath$draw_panel()}, which returns a \code{polylineGrob} with nothing named
around it, while \code{GeomSmooth} and \code{GeomDensity} wrap theirs in
\code{geom_smooth.gTree} / \code{geom_density.gTree} and are skipped whole.
}
\details{
Counting only the line-ish types therefore counted a population one
smaller than the one being indexed, and a \code{geom_function()} drawn \emph{before}
a \code{geom_line()} handed the line the function's curve to highlight (#204).
Both charts read correctly the whole time, which is the highlight-only
shape xability/maidr#814 names.
}
\keyword{internal}
