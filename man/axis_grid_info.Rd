% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axes_utils.R
\name{axis_grid_info}
\alias{axis_grid_info}
\title{Grid navigation bounds for one axis}
\usage{
axis_grid_info(built, axis = "x", panel_id = NULL)
}
\arguments{
\item{built}{Built plot data}

\item{axis}{Character, either "x" or "y"}

\item{panel_id}{Panel index for faceted plots (optional, defaults to 1)}
}
\value{
List with min, max, tickStep, or NULL
}
\description{
\code{min}, \code{max} and \code{tickStep} for the axis named, read off the built plot's
panel parameters, or \code{NULL} when any of the three cannot be determined --
which leaves the axis with its label and no grid, the graceful degradation
#158 settled on.
}
\details{
Lifted out of \code{Ggplot2PointLayerProcessor}, which still calls it, when the
rug processor came to need the same answer for the axis its ticks stand on
(#222). Two readings of one grid rule is how the two would drift.
}
\keyword{internal}
