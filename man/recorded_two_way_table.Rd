% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_r_arg_utils.R
\name{recorded_two_way_table}
\alias{recorded_two_way_table}
\title{The two-way contingency table a recorded call was handed, when it is one}
\usage{
recorded_two_way_table(args)
}
\arguments{
\item{args}{Recorded argument list, or NULL}
}
\value{
A 2-D table with named margins, or NULL
}
\description{
\code{mosaicplot()} is given the table itself, so the recorded call carries
every number a \code{mosaic} layer wants -- the counts, the margins they imply,
and the level names from \code{dimnames()}. Nothing is inferred from the
drawing.
}
\details{
Only a two-dimensional table is returned. \code{mosaicplot()} accepts three and
more, splitting recursively, and a \code{mosaic} layer has one category axis and
one fill -- so a deeper table has nowhere to put its later dimensions and
is declined rather than flattened into a cross-classification the chart
does not claim. A table with unnamed margins is declined too: the levels
are what a reader navigates by, and positions are not levels.

\code{mosaicplot()}'s other calling style hands it a formula and a \code{data}
argument instead, and \code{mosaicplot.formula()} builds the table from the two.
That is recovered by \code{formula_two_way_table()} rather than declined, for
the reason the direct call is read at all: the table is the one the chart
draws, arrived at by the same code, not a plausible reconstruction (#248).

Shared by the adapter's dispatch and the processor's extraction so the two
cannot disagree about which calls are readable.
}
\keyword{internal}
