% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_r_arg_utils.R
\name{recorded_formula}
\alias{recorded_formula}
\title{The formula a recorded call carries, resolved}
\usage{
recorded_formula(args, call_env = NULL)
}
\arguments{
\item{args}{Recorded argument list}

\item{call_env}{The environment snapshot a deferred call was recorded
with, or NULL when every argument is a plain value.}
}
\value{
The formula object, or NULL when the call carries none
}
\description{
The formula is the \code{formula} argument, the \code{x} argument, or the first
positional one, whichever is found first. On the NSE path it arrives as
the unevaluated call to \code{~}, or -- \verb{fmla <- y ~ x; plot(fmla, ...)} -- as
the name it was bound to, and either is resolved in the snapshot the
call was recorded with. Only a name or a \code{~} call is evaluated: an
arbitrary expression in the data slot (\code{plot(rnorm(10))}) is not a
formula and is not run again to find out.
}
\keyword{internal}
