% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot2_system_init.R
\name{package_masks_maidr}
\alias{package_masks_maidr}
\title{Is a package attached ahead of maidr on the search path?}
\usage{
package_masks_maidr(package)
}
\arguments{
\item{package}{Name of the package, as in \link{WRAPPED_SUGGESTS}.}
}
\value{
\code{TRUE} when both packages are attached and \code{package} comes first.
}
\description{
\code{library(quantmod)} after \code{library(maidr)} puts \code{package:quantmod} in
front of \code{package:maidr}, so an unqualified \code{chartSeries()} binds to
quantmod's own function and maidr's recording wrapper is never entered.
\code{library(vioplot)} and \code{library(wordcloud)} after maidr do the same to
\code{vioplot()} and \code{wordcloud()}: the "No Base R plots detected" error that
follows a bare call to either was measured with maidr 0.5.0 (#320).
}
\details{
maidr deliberately does not reach into another package's namespace to
win this race: overwriting a foreign package's binding would also
redirect the package's \emph{internal} calls through maidr's \code{...}-forwarding
wrapper, which for quantmod corrupts the \code{match.call(expand.dots = TRUE)}
it relies on. maidr reports the condition instead.
}
\keyword{internal}
