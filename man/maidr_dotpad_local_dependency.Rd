% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dotpad_config.R
\name{maidr_dotpad_local_dependency}
\alias{maidr_dotpad_local_dependency}
\title{The local SDK dependency, when a document going offline should carry one}
\usage{
maidr_dotpad_local_dependency(use_cdn = NULL)
}
\arguments{
\item{use_cdn}{The document's \code{use_cdn}, with \code{NULL} meaning \code{FALSE}}
}
\value{
An \code{htmltools::htmlDependency()}, or \code{NULL}
}
\description{
Applies only to \code{use_cdn = FALSE}: that is the document whose reader has no
network, and the one whose \verb{lib/} folder already travels with it. A session
that names its own copy by URL keeps that -- a configured URL wins -- and
one that never downloaded the SDK gets exactly what it did before.
}
\keyword{internal}
