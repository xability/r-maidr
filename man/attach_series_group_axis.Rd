% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/series_group_utils.R
\name{attach_series_group_axis}
\alias{attach_series_group_axis}
\title{Add the legend title as the z axis label for a grouped layer}
\usage{
attach_series_group_axis(
  axes,
  plot,
  built,
  data,
  layer_index = NULL,
  aes_groups = list(c("colour", "color"))
)
}
\arguments{
\item{axes}{Axes built so far}

\item{plot}{The ggplot2 object}

\item{built}{Built plot data (optional)}

\item{data}{The extracted layer data}

\item{layer_index}{Index of the layer being described}

\item{aes_groups}{Grouping aesthetics to probe, as documented on
\code{resolve_series_group_mapping()}}
}
\value{
The axes list, with z added when the layer is grouped
}
\description{
A grouped layer emits a per-series \code{z} value (the group's name), and
MAIDR announces it as "<z label> is <z value>". Without a z label the
frontend falls back to the generic word "Group", losing the legend title the
plot actually shows. Single-series layers emit no z value at all, so they
get no z label either.
}
\keyword{internal}
