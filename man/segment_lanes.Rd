% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot2_gantt_layer_processor.R
\name{segment_lanes}
\alias{segment_lanes}
\title{Group a layer's segments into the lanes they were drawn in}
\usage{
segment_lanes(built_data, lane_axis, lane_names = NULL)
}
\arguments{
\item{built_data}{A layer's computed data, one row per drawn segment}

\item{lane_axis}{\code{"y"} or \code{"x"}, as \code{segment_lane_axis()} returns}

\item{lane_names}{The lane names in drawn order, or NULL on a continuous
lane axis. Position \code{i} on the axis is \code{lane_names[[i]]}}
}
\value{
A list with \code{data} (lanes, each a list of \code{x}/\code{start}/\code{end}
intervals), \code{lanes} (the names of every lane in drawn order, or NULL) and
\code{order} (the built-data row behind each interval, in emission order)
}
\description{
Group a layer's segments into the lanes they were drawn in
}
\keyword{internal}
