% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_r_adapter.R
\name{warn_fourfoldplot_declined}
\alias{warn_fourfoldplot_declined}
\title{Say why a fourfoldplot() call is falling back to a picture}
\usage{
warn_fourfoldplot_declined(reason)
}
\arguments{
\item{reason}{One of \code{"std"}, \code{"strata"} or \code{"table"}.}
}
\value{
Invisibly NULL.
}
\description{
The generic "Plot contains unsupported elements" the fallback already
emits is true and uninformative: it does not tell an author that the same
chart with \code{std = "ind.max"} would have been read. This says which of the
three measured refusals applied.
}
\details{
The three reasons, all measured on R 4.3.3:
\itemize{
\item \code{"std"} -- the caller's \code{std} resolved to \code{"margins"}, which is
\code{graphics::fourfoldplot}'s own default. Under it the four radii are
\code{sqrt(c(u, 1 - u, 1 - u, u))} with \code{u = sqrt(or) / (1 + sqrt(or))}:
one number, the odds ratio, drawn four times. Measured, a table and the
same table times three give bit-identical radii, so counts announced
there would name numbers the chart did not draw.
\item \code{"strata"} -- a 2x2xk array. \code{fourfoldplot()} draws k panels into one
figure region with repeated \code{plot.window()} calls rather than
\code{par(mfrow)}, so measured on \code{UCBAdmissions} all 78 polygon grobs are
named \verb{graphics-plot-1-*} and no helper here slices them per panel.
The MESSAGE names the third dimension rather than the k panels, because
"k panels share one region" is false for the one array shape that has
no strata to confuse: measured, \code{array(c(10, 40, 90, 160), c(2, 2, 1))}
under \code{ind.max} draws a single panel of exactly 13 polygon grobs -- the
count \code{wedge_names()} accepts -- and is declined anyway, because
\code{recorded_two_way_table()} refuses three dimensions. Telling that author
about panels they do not have would send them looking for the wrong
thing; the matrix spelling is the fix and the message says so.
\item \code{"table"} -- a two-way argument that is not a 2x2 table of finite
non-negative numbers summing above zero. Measured, the all-zero table
draws ZERO polygon grobs, and a logical matrix prints \code{TRUE}/\code{FALSE}
on the page while \code{as.numeric()} would announce \code{1}/\code{0}.
}

Once per reason per session, not once per plot: \code{detect_layer_type()} is
called up to five times for one accepted layer
(\code{base_r_plot_orchestrator.R} lines 145, 163, 195, 499, 653) and once per
declined one, so an unguarded warning would repeat. The cost is that an
author who draws two default-\code{std} charts hears the explanation once --
the same trade \code{warn_chartseries_ta_unsupported()} makes above.

The message deliberately avoids the substring "unsupported elements":
\code{tests/testthat/test-base-r-unrecorded-calls.R} greps for exactly that to
decide whether the \emph{fallback} warning arrived, and a second warning
carrying it would make that assertion pass for the wrong reason.
}
\keyword{internal}
