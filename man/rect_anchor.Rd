% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svg_utils.R
\name{rect_anchor}
\alias{rect_anchor}
\title{Where a rect grob is anchored on one axis, as a fraction}
\usage{
rect_anchor(grob, axis)
}
\arguments{
\item{grob}{A rect grob}

\item{axis}{\code{"horizontal"} or \code{"vertical"}}
}
\value{
The anchor as a fraction: 0 is the low edge, 1 the high one
}
\description{
A \code{rectGrob()} keeps its justification in \code{just} and leaves \code{hjust} /
\code{vjust} NULL unless the caller wrote them, so neither field alone answers
the question. \code{just} may be a keyword, a number, or a length-two vector of
either; absent, \code{grid}'s own default is \code{"centre"}.
}
\details{
Anything unrecognised answers 0.5, the default -- an anchor this cannot
read is one it should not move.
}
\keyword{internal}
