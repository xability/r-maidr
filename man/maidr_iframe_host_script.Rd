% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svg_utils.R
\name{maidr_iframe_host_script}
\alias{maidr_iframe_host_script}
\title{Parent-side listener for the messages a MAIDR iframe posts}
\usage{
maidr_iframe_host_script()
}
\value{
Character string with a script tag
}
\description{
Handles two messages, both keyed to the frame that sent them by matching
\code{contentWindow} against the message's source:
}
\details{
\itemize{
\item \code{maidr-iframe-height} resizes the frame to its content.
\item \code{maidr:frame-focus-escape} moves focus out of the frame and into this
document. Keyboard events do not cross a frame boundary, so while the
reader is inside the chart the page around it hears nothing --- and
Shift+Tab at the chart, the frame's first tab stop, leaves the document
altogether rather than landing on the page, because there is nothing
before the frame to receive focus. On a reveal.js slide that means no key
reaches the deck: neither Space for the next slide nor the arrows for the
previous one. The chart is embedded through a \verb{data:} URL, whose opaque
origin puts it beyond the reach of this document's DOM, so it asks for the
handoff instead of performing it.
}

Focus lands on the element holding the frame --- a reveal.js slide is a
\verb{<section>}, so on a slide deck that is the slide itself --- given
\code{tabindex="-1"} so it can take focus without joining the tab order.

Registered at most once per document (guarded by a window flag), no
matter how many iframes embed it.
}
\keyword{internal}
