% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html_dependencies.R
\name{maidr_cdn_loader_script}
\alias{maidr_cdn_loader_script}
\title{Script that loads maidr.js from the CDN, falling back to the page's copy}
\usage{
maidr_cdn_loader_script(cdn_js_url)
}
\arguments{
\item{cdn_js_url}{URL of maidr.js on the CDN}
}
\value{
Character string holding a \verb{<script>} element
}
\description{
Goes into a chart frame's \code{srcdoc} in place of a plain CDN \verb{<script>}. When
the CDN load fails -- offline, blocked -- it looks in the parent document
for the copy \code{\link[=maidr_page_bundle_dependency]{maidr_page_bundle_dependency()}} put there. A \code{srcdoc} frame
shares its parent's origin, so it can read it.
}
\details{
The copy arrives in one of two shapes, and both are handled: a \code{src} (the
\verb{_files} folder, or a \verb{data:} URL) or inline text, which is what pandoc
turns a script into when it embeds it. maidr.js initialises itself when it
runs, so nothing is called once it has loaded. KaTeX, which it fetches only
when an AI response carries maths, is pointed at the page's copy through
\code{window.maidrMathStylesheetUrl}, or added as a \verb{<style>} when inline.
}
\keyword{internal}
