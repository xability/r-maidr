% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/box_order_utils.R
\name{reverse_horizontal_box_layer}
\alias{reverse_horizontal_box_layer}
\title{Turn a horizontal box-family layer round for the frontend}
\usage{
reverse_horizontal_box_layer(layer)
}
\arguments{
\item{layer}{A layer list carrying \code{data}, \code{selectors} and \code{orientation}}
}
\value{
The layer, with both halves reversed when it is horizontal
}
\description{
\code{BoxTrace}, \code{ViolinBoxTrace} and \code{ViolinTrace} each reverse a horizontal
layer on the way in -- "reverse points to match visual order (lower-left
start)", as \code{src/model/box.ts} puts it. The reversal is unconditional, so
the producer has to hand them the \emph{opposite} order for the result to come
out lower-left first, and a layer emitted in its own natural bottom-to-top
order is read from the top down instead.
}
\details{
Both halves move together. The frontend reverses the resolved highlight
alongside the points (\code{BoxTrace} explicitly, \code{ViolinTrace} by reversing the
selectors before it resolves them), so a layer that reversed only its data
would trade a correct outline for a wrong one.

Only the emission order changes. Each entry keeps its own category name and
its own statistics, and \code{orientation} still says which axis the values are
drawn along.

Worth stating what this does not settle: the frontend's reversal may exist
\emph{because} py-maidr reverses, in which case the cleaner fix is to drop it
there and let every producer emit in its own natural order. That is a
three-repository change and a released-behaviour change for py-maidr
readers, so it is left to the maintainer. Until then the two bindings agree,
which is the part a reader can feel.
}
\keyword{internal}
