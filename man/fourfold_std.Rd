% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_r_adapter.R
\name{fourfold_std}
\alias{fourfold_std}
\title{Resolve a recorded \code{std} the way \code{fourfoldplot()} itself resolves it}
\usage{
fourfold_std(args)
}
\arguments{
\item{args}{The arguments recorded from the \code{fourfoldplot()} call.}
}
\value{
One of \code{"margins"}, \code{"ind.max"} or \code{"all.max"}.
}
\description{
\code{graphics::fourfoldplot} runs \code{std <- match.arg(std)}, so partial spellings
are legal calls that draw the counts. Measured on R 4.3.3, every one of
these is admitted:
}
\details{
\preformatted{
  <absent> -> margins   "margins" -> margins   "m"   -> margins
  "ma"     -> margins   "ind.max" -> ind.max   "ind" -> ind.max
  "i"      -> ind.max   "in"      -> ind.max   "all.max" -> all.max
  "all"    -> all.max   "a"       -> all.max
  c("margins", "ind.max", "all.max") -> margins
}

So \code{identical(args[["std"]], "ind.max")} would silently decline five legal
spellings. The one existing exact-comparison idiom in this package,
\code{base_r_subseries_layer_processor.R}'s \code{spikes()}, is complete only because
\code{monthplot}'s \code{type} choices are the single characters \code{"l"} and \code{"h"},
where partial matching cannot produce a non-choice string. \code{std}'s choices
are multi-character, so the same idiom is incomplete here.

Anything \code{match.arg()} rejects resolves to \code{"margins"}, which declines:
measured, \code{"IND.MAX"}, \code{"x"}, \code{NA_character_}, \code{character(0)} and
\code{c("ind.max", "margins")} all error inside \code{match.arg()}, and
\code{fourfoldplot()} itself raises the identical error first, so none of them
is reachable from a drawn chart. The \code{tryCatch} is there so that a reader
never \code{stop()}s and takes the whole figure with it -- the reason
\code{recorded_flag()} gives for the same shape.

A non-character \code{std} is refused before \code{match.arg()} rather than coerced.
Measured, \code{fourfoldplot(tb, std = 1L)}, \code{std = TRUE} and
\code{std = factor("ind.max")} all stop with "'arg' must be NULL or a character
vector"; \code{as.character(factor("ind.max"))} would have been accepted here
and would have read a call that upstream refuses to draw.

\code{args[["std"]]}, not \code{args$std}: \code{$} partial-matches a list, the collision
recorded in \code{recorded_main_title()}.
}
\keyword{internal}
