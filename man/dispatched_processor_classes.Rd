% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processor_factory.R
\name{dispatched_processor_classes}
\alias{dispatched_processor_classes}
\title{The processor classes a factory's \code{create_processor()} dispatches to}
\usage{
dispatched_processor_classes(generator, prefix)
}
\arguments{
\item{generator}{The factory's \code{R6ClassGenerator}.}

\item{prefix}{The class-name prefix its processors share.}
}
\value{
Character vector of class names, in the order first dispatched.
}
\description{
Read off the method rather than kept beside it. The list a factory used to
return was written out by hand and consulted by nothing, so it drifted
freely: by the time #200 was filed it was missing four of the twenty
ggplot2 processors and one of the fourteen base R ones, and nothing in the
package or its tests could tell. A second list is a second thing to keep
true; asking the dispatch itself is one thing that cannot disagree with
itself.
}
\details{
\code{deparse(body(...))} is used rather than reading \verb{R/}, because an installed
package has no \verb{R/} to read -- the sources are gone by then and only the
parsed function survives.
}
\keyword{internal}
