% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base_r_spineplot_layer_processor.R
\name{spineplot_written_axis_names}
\alias{spineplot_written_axis_names}
\title{Name a replayed \code{spineplot(x, y)}'s axes the way the caller's call did}
\usage{
spineplot_written_axis_names(args, call_expr)
}
\arguments{
\item{args}{Recorded argument list}

\item{call_expr}{The recorded call, deparsed}
}
\value{
\code{args}, with \code{xlab}/\code{ylab} filled in where the call named them
}
\description{
\code{spineplot.default()} titles its axes \code{deparse1(substitute(x))} and
\code{deparse1(substitute(y))} when no \code{xlab}/\code{ylab} is given. Replayed through
\code{do.call()} on the recorded \emph{values}, the substitute is the data itself,
and a reader was told the axis was called \code{c(1, 2, 3, ...)} for as many
characters as the vector took to write. The names the caller wrote are in
the recorded call text, so they are matched against the default method's
formals and passed as the titles. A table or a formula names its own
axes and is left alone.
}
\keyword{internal}
